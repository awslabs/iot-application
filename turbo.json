{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**"],
      "dependsOn": ["lint", "test", "^build"],
      "env": [
        "DATABASE_ENDPOINT",
        "DATABASE_NAME",
        "DATABASE_PORT",
        "DATABASE_LAUNCH_LOCAL"
      ]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "test": {
      "outputs": ["coverage/**"]
    },
    "test:watch": {
      "cache": false
    }
  }
}
